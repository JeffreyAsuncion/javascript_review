{
    // function func(x){
    //     return x*x;
    // }

    // same as above
    // the () are for 0 or more ht
    let func2 = (x) => x * x;

    // let func2 = (x) => {return x * x;}
    console.log(func2(3));
}